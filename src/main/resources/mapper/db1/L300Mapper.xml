<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.atg.apds.komipo_main.fend.mapper.db1.L300Mapper">


  <select id="getMeasptOverallStatus" resultType="kr.co.atg.apds.komipo_main.entity.pobject.P_getMeasptOverallStatus">
    SELECT mptid, mptkey, measdt, overall, units, unitstr from get_measpt_overall_status(${equipmentkey})
  </select>

  <select id="getMeasptAlarmLimit" resultType="kr.co.atg.apds.komipo_main.entity.pobject.P_getMeasptAlarmLimit">
    SELECT mptid, mptkey, alerthigh, faulthigh, units, unitstr FROM get_measpt_alarm_limit(${equipmentkey})
  </select>

</mapper>